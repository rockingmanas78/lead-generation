{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "preDeployCommand": "prisma generate",
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port 8080"
  }
}
